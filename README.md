# Frees Blog

Frees Ling 的个人数字花园，使用 Astro 构建并部署到 GitHub Pages。

## 本地运行

```bash
npm install
npm run dev
```

## 验证

```bash
npm run check
npm run lint:md
npm run build
```

文章位于 `src/content/posts/`。构建会同时生成全文搜索索引、RSS、站点地图和静态页面。
