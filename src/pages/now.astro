---
import Layout from '../layouts/Layout.astro';
import { nowItems } from '../data/site';
---
<Layout title="此刻"><header class="page-hero container"><p class="eyebrow">NOW · UPDATED 2026.09</p><h1 class="page-title">此刻，我把注意力<br />放在哪里。</h1><p class="lede">Now 页面是一张时间切片，不是履历。它会随着生活的重心一起改变。</p></header><section class="container now-board">{nowItems.map((item,index)=><article><span>0{index+1}</span><p>{item.label}</p><h2>{item.value}</h2></article>)}</section><section class="section container principles"><div><p class="eyebrow">现在相信的事</p><h2 class="section-title">现在相信的几件事</h2></div><ul><li>先把问题说清楚，再急着寻找答案。</li><li>没有证据的结论，应当诚实地标记为猜想。</li><li>工具是为了放大表达，而不是替代表达。</li><li>允许作品生长，也允许自己改变方向。</li></ul></section></Layout>
<style>.now-board{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px;padding-bottom:100px}.now-board article{min-height:290px;padding:30px;border:1px solid var(--line);border-radius:var(--radius);background:var(--surface)}.now-board span{color:var(--signal-text);font-family:var(--font-display);font-size:38px}.now-board p{margin:50px 0 6px;color: var(--ink-soft);font-family:var(--font-mono);font-size:10px;letter-spacing:.12em}.now-board h2{margin:0;font-family:var(--font-display);font-size:clamp(20px,2.4vw,28px);font-weight:500;line-height:1.2}.principles{display:grid;grid-template-columns:.8fr 1.2fr;gap:80px;border-top:1px solid var(--line)}.principles ul{margin:0;padding:0;list-style:none}.principles li{padding:20px 0;border-bottom:1px solid var(--line);font-family:var(--font-display);font-size:22px}@media(max-width:900px){.now-board{grid-template-columns:1fr}.principles{grid-template-columns:1fr}}</style>
